{% extends 'base.html.twig' %}

{% block title %}Perimeter{% endblock %}

{% block body %}
    <h1>Perimeter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ perimeter.id }}</td>
            </tr>
            <tr>
                <th>RefNumber</th>
                <td>{{ perimeter.refNumber }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ perimeter.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ perimeter.longitude }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('perimeter_index') }}">back to list</a>

    <a href="{{ path('perimeter_edit', {'id': perimeter.id}) }}">edit</a>

    {{ include('perimeter/_delete_form.html.twig') }}
{% endblock %}
