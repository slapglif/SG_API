{% extends 'base.html.twig' %}

{% block title %}CheckPoint{% endblock %}

{% block body %}
    <h1>CheckPoint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ check_point.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ check_point.name }}</td>
            </tr>
            <tr>
                <th>AssetId</th>
                <td>{{ check_point.assetId }}</td>
            </tr>
            <tr>
                <th>LocationInformation</th>
                <td>{{ check_point.locationInformation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('check_point_index') }}">back to list</a>

    <a href="{{ path('check_point_edit', {'id': check_point.id}) }}">edit</a>

    {{ include('check_point/_delete_form.html.twig') }}
{% endblock %}
